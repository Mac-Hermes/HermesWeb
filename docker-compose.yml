version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/prisma/dev.db
      - JWT_SECRET=change-me
      - WHATSAPP_TOKEN=
      - WHATSAPP_PHONE_ID=
      - NEXT_PUBLIC_BASE_URL=http://jg46est9pg5lmgdo110a2did.80.75.221.24.sslip.io
    restart: unless-stopped
